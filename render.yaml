services:
  - type: web
    name: 4digit-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false
      - key: FLASK_DEBUG
        value: "0"
